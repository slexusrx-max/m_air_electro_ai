import type { PowerPlantsRepository } from "@/repositories/power-plants/types";
/** Future adapter: keep UI independent from the persistence provider. */
export const supabasePowerPlantsRepository: Partial<PowerPlantsRepository> = {};
