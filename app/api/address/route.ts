import { UkrposhtaAddressProvider } from "@/lib/address/ukrposhta";
export async function GET(request:Request){const u=new URL(request.url);const mode=u.searchParams.get("mode");const query=(u.searchParams.get("q")??"").trim();try{const p=new UkrposhtaAddressProvider();if(mode==="settlements")return Response.json(await p.searchSettlements(query));if(mode==="streets")return Response.json(await p.getStreets(u.searchParams.get("settlementId")??"",query));if(mode==="buildings")return Response.json(await p.getBuildings(u.searchParams.get("streetId")??"",query));return Response.json({error:"Invalid address query"},{status:400});}catch{return Response.json({error:"Address provider unavailable"},{status:503});}}
