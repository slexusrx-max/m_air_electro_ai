import { PlatformShell } from "@/components/platform-shell";
import { ProductPage } from "@/components/product/page-content";
const prompts = ["What battery do I need for an 8-hour outage?", "Can a 5 kWh battery power my apartment?", "What inverter should I use?", "How long will my refrigerator, router and boiler run?"];
export default function EnergyAiPage() { return <PlatformShell><ProductPage eyebrow="Energy AI" title="Ask Energy AI." description="Specialist guidance for backup power, batteries, solar and household electricity — prepared to use your My Home context when it is connected."><section className="info-card rounded-3xl p-6"><textarea className="min-h-40 w-full rounded-2xl border border-slate-200 bg-white p-4" placeholder="Ask about your backup system…"/><div className="mt-4 flex flex-wrap gap-2">{prompts.map((prompt) => <button key={prompt} type="button" className="rounded-full border border-teal-200 bg-teal-50 px-3 py-2 text-sm text-teal-900">{prompt}</button>)}</div><p className="mt-5 text-sm text-slate-600">AI responses and home context require an enabled AI backend; this interface does not pretend to provide a live consultation without one.</p></section></ProductPage></PlatformShell>; }
