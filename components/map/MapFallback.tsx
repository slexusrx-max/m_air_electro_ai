import type { PowerPlant } from "@/types/power";
export function MapFallback({ plants, onSelect }: { plants: PowerPlant[]; onSelect: (plant: PowerPlant) => void }) { return <div className="map-fallback flex min-h-[420px] flex-col p-5"><div className="rounded-2xl border border-cyan-100 bg-white/85 p-4 text-sm text-slate-700"><strong className="text-slate-950">Map service is not configured.</strong><p className="mt-1">Set NEXT_PUBLIC_MAP_STYLE_URL to load the interactive map. The demonstration facilities remain available below.</p></div><div className="mt-5 grid flex-1 content-start gap-2 overflow-auto sm:grid-cols-2">{plants.map((plant) => <button key={plant.id} onClick={() => onSelect(plant)} className="rounded-xl border border-slate-200 bg-white/75 px-3 py-3 text-left text-sm text-slate-700 transition hover:border-teal-300 focus-visible:outline-teal-600"><span className="block font-semibold text-slate-950">{plant.name}</span><span>{plant.country} · {plant.source} · {plant.installedCapacityMw?.toLocaleString()} MW</span></button>)}</div></div>; }
