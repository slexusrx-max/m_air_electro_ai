"use client";
import { useEffect, useState } from "react";
import { searchPowerInfrastructure } from "@/services/power-plants";
import type { SearchResult } from "@/types/power";
export function MapSearch({ onSelect }: { onSelect: (result: SearchResult) => void }) { const [query, setQuery] = useState(""); const [results, setResults] = useState<SearchResult[]>([]); useEffect(() => { const id = window.setTimeout(() => { void searchPowerInfrastructure(query).then(setResults); }, 180); return () => window.clearTimeout(id); }, [query]); return <div className="relative"><label htmlFor="map-search" className="sr-only">Search power infrastructure</label><input id="map-search" value={query} onChange={(event) => setQuery(event.target.value)} onKeyDown={(event) => { if (event.key === "Escape") { setQuery(""); setResults([]); } }} placeholder="Search country, city, region or power plant" className="w-full rounded-2xl border border-slate-200 bg-white/85 px-4 py-3 pr-10 text-sm text-slate-900 shadow-sm outline-none placeholder:text-slate-400 focus:border-teal-500"/>{query && <button aria-label="Clear search" onClick={() => setQuery("")} className="absolute right-3 top-2.5 rounded-lg px-2 py-1 text-slate-500 hover:bg-slate-100">×</button>}{results.length > 0 && <ul role="listbox" className="absolute z-30 mt-2 max-h-72 w-full overflow-auto rounded-2xl border border-white bg-white/95 p-2 shadow-xl">{results.map((result) => <li key={`${result.type}-${result.id}`}><button onClick={() => { onSelect(result); setQuery(""); setResults([]); }} className="w-full rounded-xl px-3 py-2 text-left hover:bg-teal-50"><span className="block text-sm font-semibold text-slate-900">{result.label}</span><span className="text-xs text-slate-600">{result.detail}</span></button></li>)}</ul>}<p className="sr-only" aria-live="polite">{results.length} search results</p></div>; }
