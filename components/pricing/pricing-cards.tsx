"use client";
import { useState } from "react";
import { startCheckout } from "@/lib/billing/client";
const tiers = [{ id: "free", name: "Free", price: "$0", items: ["World power plant map", "Basic facility information", "One saved location", "One basic alert"] }, { id: "premium-monthly", name: "Premium", price: "$5.99/month", items: ["Advanced regional analysis", "Up to 10 saved locations", "Location comparison", "Outage and status alerts"] }, { id: "professional", name: "Professional", price: "Contact us", items: ["Advanced datasets", "Exports and API access", "Team access", "Priority support"] }];
export function PricingCards() { const [message, setMessage] = useState(""); return <><div className="grid gap-4 lg:grid-cols-3">{tiers.map((tier) => <article key={tier.id} className={`info-card ${tier.id === "premium-monthly" ? "border-teal-400 ring-1 ring-teal-200" : ""}`}><h2>{tier.name}</h2><p className="mt-2 text-3xl font-bold text-slate-950">{tier.price}</p><ul className="mt-5 space-y-2 text-sm text-slate-700">{tier.items.map((item) => <li key={item}>✓ {item}</li>)}</ul><button onClick={() => void startCheckout(tier.id).catch((error: Error) => setMessage(error.message))} className="button-primary mt-6">{tier.id === "free" ? "Included" : tier.id === "professional" ? "Contact us" : "Choose Premium"}</button></article>)}</div>{message && <div role="status" className="mt-5 rounded-2xl border border-cyan-200 bg-cyan-50 p-4 text-sm text-cyan-950">{message}</div>}</>; }
