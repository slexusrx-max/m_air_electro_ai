"use client";
import { useState } from "react";
import Link from "next/link";
import { solarEstimate } from "@/lib/marketplace/query";
export function SolarCalculator({ro}:{ro:boolean}){const [daily,setDaily]=useState("3000");const [sun,setSun]=useState("3.5");const [factor,setFactor]=useState("75");const watts=solarEstimate(Number(daily),Number(sun),Number(factor)/100);return <section className="content-panel"><div className="compare-selectors"><label>{ro?"Consum zilnic (Wh)":"Daily energy (Wh)"}<input type="number" min="1" value={daily} onChange={e=>setDaily(e.target.value)}/></label><label>{ro?"Ore solare echivalente / zi":"Peak sun hours / day"}<input type="number" min="0.1" max="12" step="0.1" value={sun} onChange={e=>setSun(e.target.value)}/></label><label>{ro?"Factor de producție (%)":"System yield factor (%)"}<input type="number" min="1" max="100" value={factor} onChange={e=>setFactor(e.target.value)}/></label></div><div className="calculation-result" aria-live="polite">{watts===null?(ro?"Introdu valori pozitive: ore ≤ 12 și factor ≤ 100%.":"Enter positive values: hours ≤ 12 and factor ≤ 100%."):<><span>{ro?"Putere orientativă panouri":"Estimated array power"}</span><strong>{watts.toLocaleString(ro?"ro-RO":"en-GB")} W</strong><p>{daily} Wh ÷ {sun} h ÷ ({factor} / 100)</p></>}</div><p>{ro?"Exemplul inițial nu este prognoză pentru România. Alege orele după locație și luna de utilizare din PVGIS. Include pierderile o singură dată; dimensionează separat stocarea și regulatorul. Estimarea nu include recuperarea după mai multe zile fără soare.":"The initial example is not a Romania forecast. Choose hours for the location and design month from PVGIS. Count losses once; size storage and the controller separately. The estimate excludes recovery after several low-solar days."}</p><div className="action-row"><Link className="button-primary" href="/marketplace/solar/panels">{ro?"Compară panouri":"Compare panels"}</Link><Link href="/marketplace/solar/controllers">{ro?"Regulatoare":"Controllers"} →</Link><Link href="/learn/solar-panel-sizing">{ro?"Metoda și limitele":"Method and limitations"} →</Link></div></section>;}
